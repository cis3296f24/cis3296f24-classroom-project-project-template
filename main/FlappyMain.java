package main;

/**
 * Main entry point for the Flappy Bird game.
 * This class initiates the game by creating an instance of the FlappyGame class.
 */
public class FlappyMain {

	/**
	 * The main method to start the Flappy Bird game.
	 *
	 * @param args command-line arguments (not used in this application)
	 */
	public static void main(String[] args) {
		// Create an instance of FlappyGame to start the game
		new FlappyGame();
	}
}
